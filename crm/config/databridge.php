<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Моніторинг статусу підключення (A-2, SC-002/SC-003)
    |--------------------------------------------------------------------------
    | Значення налаштовувані — це базові дефолти.
    */

    // Очікуваний інтервал звітування плагіна (секунди).
    'heartbeat_interval' => (int) env('DB_HEARTBEAT_INTERVAL', 60),

    // Тиша, після якої сайт позначається офлайн (секунди). ~3 пропущені звіти.
    'offline_window' => (int) env('DB_OFFLINE_WINDOW', 300),

    /*
    |--------------------------------------------------------------------------
    | Підпис запитів (contract §1; A-4, FR-011)
    |--------------------------------------------------------------------------
    */

    // Версія схеми підпису (X-DB-Sig-Version).
    'sig_version' => env('DB_SIG_VERSION', 'v1'),

    // Логічний шлях, що входить у канонічний рядок і приймає heartbeat.
    'heartbeat_path' => env('DB_HEARTBEAT_PATH', '/v1/heartbeat'),

    // Вікно толерантності часу (секунди, ±).
    'timestamp_tolerance' => (int) env('DB_TIMESTAMP_TOLERANCE', 300),

    // TTL nonce у анти-replay сторі (секунди). МАЄ бути 2× вікна толерантності.
    'nonce_ttl' => (int) env('DB_NONCE_TTL', 600),

    /*
    |--------------------------------------------------------------------------
    | Rate-limiting (FR-026) — 2-й контур на бекенді (per site-id)
    |--------------------------------------------------------------------------
    */

    'rate_limit' => [
        // Бекенд (другий контур). Проксі тримає перший контур (~10/min burst 20).
        'backend_per_minute' => (int) env('DB_RATE_BACKEND_PER_MIN', 6),
    ],

    // Максимальний розмір тіла heartbeat (байти) — edge sanity, FR-027.
    'max_body_bytes' => (int) env('DB_MAX_BODY_BYTES', 8192),

];
